# Cat_Sys CRM ????

## ????

| ?? | ??? | IP | ?? | ?? |
|------|--------|-----|------|------|
| ?? | Rocky Linux 9 VM | 10.0.0.120 | ? | ??????K3s |
| ?? | ????? | 67.219.106.150 | xui6.bbbus.top/cat | ???? |

## ?????: Docker Compose (??)

```bash
# 1. ????
cp config/development.env .env

# 2. ??????
cd deploy
docker-compose up -d --build

# 3. ????
docker-compose ps

# 4. ????
docker-compose logs -f api-gateway
```

## ?????: K3s (??)

### ????

```bash
# Rocky Linux 9 ?? K3s
curl -sfL https://get.k3s.io | sh -

# ??
sudo k3s kubectl get nodes
```

### ????

```bash
# 1. ??????????????????
kubectl apply -f deploy/k3s/namespace.yaml
kubectl apply -f deploy/k3s/configmap.yaml
kubectl apply -f deploy/k3s/secrets.yaml

# 2. ?? MySQL ? Redis
kubectl apply -f deploy/k3s/mysql/
kubectl apply -f deploy/k3s/redis/

# 3. ???????? registry
# (???? docker save/load ?? registry ???)
docker build -t cat-sys/api-gateway:v0.1 ./api-gateway
docker build -t cat-sys/user-service:v0.1 ./services/user-service
# ... ??????

# 4. ?????
kubectl apply -f deploy/k3s/services/

# 5. (??) ?? Ingress
kubectl apply -f deploy/k3s/ingress.yaml

# 6. (?? VM) ?? NodePort ??
# http://10.0.0.120:30002/cat/...
```

### ??????

1. ?? `deploy/k3s/configmap.yaml` ?? DOMAIN ? SERVER_HOST
2. ?? `deploy/k3s/secrets.yaml` ???????
3. ?? apply ??

```bash
kubectl apply -f deploy/k3s/configmap.yaml
kubectl apply -f deploy/k3s/secrets.yaml
kubectl rollout restart deployment -n cat-sys
```
