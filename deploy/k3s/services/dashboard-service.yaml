apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-service
  namespace: cat-sys
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard-service
  template:
    metadata:
      labels:
        app: dashboard-service
    spec:
      containers:
      - name: dashboard-service
        image: cat-sys/dashboard-service:v0.1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5006
        envFrom:
        - configMapRef:
            name: cat-sys-config
        - secretRef:
            name: cat-sys-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 5006
          initialDelaySeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 5006
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: cat-sys
spec:
  selector:
    app: dashboard-service
  ports:
  - port: 5006
    targetPort: 5006
