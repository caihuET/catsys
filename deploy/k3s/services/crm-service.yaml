apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-service
  namespace: cat-sys
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crm-service
  template:
    metadata:
      labels:
        app: crm-service
    spec:
      containers:
      - name: crm-service
        image: cat-sys/crm-service:v0.1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5004
        envFrom:
        - configMapRef:
            name: cat-sys-config
        - secretRef:
            name: cat-sys-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 5004
          initialDelaySeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 5004
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: crm-service
  namespace: cat-sys
spec:
  selector:
    app: crm-service
  ports:
  - port: 5004
    targetPort: 5004
