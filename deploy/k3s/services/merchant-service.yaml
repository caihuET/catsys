apiVersion: apps/v1
kind: Deployment
metadata:
  name: merchant-service
  namespace: cat-sys
spec:
  replicas: 1
  selector:
    matchLabels:
      app: merchant-service
  template:
    metadata:
      labels:
        app: merchant-service
    spec:
      containers:
      - name: merchant-service
        image: cat-sys/merchant-service:v0.1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5002
        envFrom:
        - configMapRef:
            name: cat-sys-config
        - secretRef:
            name: cat-sys-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 5002
          initialDelaySeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 5002
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: merchant-service
  namespace: cat-sys
spec:
  selector:
    app: merchant-service
  ports:
  - port: 5002
    targetPort: 5002
